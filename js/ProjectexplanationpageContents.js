const web = "webdev.png";
const webdevLink = "https://en.wikipedia.org/wiki/Web_development";
const cppLink = "https://ko.wikipedia.org/wiki/C%2B%2B";
const cpp = "C++.svg";
export let PagePContents = [
  {
    id: "1",
    title: "32. Search in Rotated Sorted Array",
    date: "Novermber 25, 2025",
    difficulty: 3,
    tag: "#HTML, CSS, JS #Webdev",
    type: web,
    link1: webdevLink,
    content: [
      `The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.`,
    ],
    code: [
      `#include <iostream>
using namespace std;

int main() {
  cout << "Hello, World!" << endl;
  return 0;
}
  `,
    ],
  },
  {
    id: "2",
    title: "33. Search in Rotated Sorted Array",
    date: "Novermber 25, 2025",
    difficulty: 3,
    tag: "#Binarysearch, #CodeForces, #gongganchaewo",
    type: cpp,
    link1: cppLink,
    content: [
      `The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.`,
    ],
    code: [
      `#include <iostream>
using namespace std;

int main() {
  cout << "Hello, World!" << endl;
  return 0;
}
  `,
    ],
  },
  {
    id: "3",
    title: "32. Search in Rotated Sorted Array",
    date: "Novermber 25, 2025",
    difficulty: 3,
    tag: "#Binarysearch, #CodeForces, #gongganchaewo",
    type: web,
    link1: webdevLink,
    content: [
      `The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.`,
    ],
    code: [
      `#include <iostream>
using namespace std;

int main() {
  cout << "Hello, World!" << endl;
  return 0;
}
  `,
    ],
  },
  {
    id: "4",
    title: "32. Search in Rotated Sorted Array",
    date: "Novermber 25, 2025",
    difficulty: 3,
    tag: "#Binarysearch, #CodeForces, #gongganchaewo",
    type: web,
    link1: webdevLink,
    content: [
      `The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.`,
    ],
    code: [
      `#include <iostream>
using namespace std;

int main() {
  cout << "Hello, World!" << endl;
  return 0;
}
  `,
    ],
  },
  {
    id: "5",
    title: "32. Search in Rotated Sorted Array",
    date: "Novermber 25, 2025",
    difficulty: 3,
    tag: "#Binarysearch, #CodeForces, #gongganchaewo",
    type: web,
    link1: webdevLink,
    content: [
      `The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.`,
    ],
    code: [
      `#include <iostream>
using namespace std;

int main() {
  cout << "Hello, World!" << endl;
  return 0;
}
  `,
    ],
  },
  {
    id: "6",
    title: "32. Search in Rotated Sorted Array",
    date: "Novermber 25, 2025",
    difficulty: 3,
    tag: "#Binarysearch, #CodeForces, #gongganchaewo",
    type: web,
    link1: webdevLink,
    content: [
      `The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.`,
    ],
    code: [
      `#include <iostream>
using namespace std;

int main() {
  cout << "Hello, World!" << endl;
  return 0;
}
  `,
    ],
  },
  {
    id: "7",
    title: "32. Search in Rotated Sorted Array",
    date: "Novermber 25, 2025",
    difficulty: 3,
    tag: "#Binarysearch, #CodeForces, #gongganchaewo",
    type: web,
    link1: webdevLink,
    content: [
      `The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.`,
    ],
    code: [
      `#include <iostream>
using namespace std;

int main() {
  cout << "Hello, World!" << endl;
  return 0;
}
  `,
    ],
  },
  {
    id: "8",
    title: "32. Search in Rotated Sorted Array",
    date: "Novermber 25, 2025",
    difficulty: 3,
    tag: "#Binarysearch, #CodeForces, #gongganchaewo",
    type: web,
    link1: webdevLink,
    content: [
      `The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.`,
    ],
    code: [
      `#include <iostream>
using namespace std;

int main() {
  cout << "Hello, World!" << endl;
  return 0;
}
  `,
    ],
  },
  {
    id: "9",
    title: "32. Search in Rotated Sorted Array",
    date: "Novermber 25, 2025",
    difficulty: 3,
    tag: "#Binarysearch, #CodeForces, #gongganchaewo",
    type: web,
    link1: webdevLink,
    content: [
      `The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.`,
    ],
    code: [
      `#include <iostream>
using namespace std;

int main() {
  cout << "Hello, World!" << endl;
  return 0;
}
  `,
    ],
  },
  {
    id: "10",
    title: "32. Search in Rotated Sorted Array",
    date: "Novermber 25, 2025",
    difficulty: 3,
    tag: "#Binarysearch, #CodeForces, #gongganchaewo",
    type: web,
    link1: webdevLink,
    content: [
      `The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.`,
    ],
    code: [
      `#include <iostream>
using namespace std;

int main() {
  cout << "Hello, World!" << endl;
  return 0;
}
  `,
    ],
  },
  {
    id: "11",
    title: "32. Search in Rotated Sorted Array",
    date: "Novermber 25, 2025",
    difficulty: 3,
    tag: "#Binarysearch, #CodeForces, #gongganchaewo",
    type: web,
    link1: webdevLink,
    content: [
      `The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.`,
    ],
    code: [
      `#include <iostream>
using namespace std;

int main() {
  cout << "Hello, World!" << endl;
  return 0;
}
  `,
    ],
  },
];
export function findPagePContent(target) {
  for (let i = 0; i < PagePContents.length; i++) {
    if (PagePContents[i].id === target) {
      return PagePContents[i];
    }
  }
}
