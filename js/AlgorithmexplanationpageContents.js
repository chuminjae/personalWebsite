const cf = "codeforces.jpg";
const lc = "leetcode.png";
const cfLink = "https://codeforces.com/";
const lcLink = "https://leetcode.com/";
export let PageAlContents = [
  {
    id: "1",
    title: "33. Search in Rotated Sorted Array",
    date: "Novermber 25, 2025",
    difficulty: 3,
    tag: "#HTML, CSS, JS #Webdev #HTML, CSS, JS #Webdev",
    type: cf,
    link1: cfLink,
    content: [
      `The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.`,
    ],
    code: [
      `#include <iostream>
using namespace std;

int main() {
  cout << "Hello, World!" << endl;
  return 0;
}
  `,
    ],
  },
  {
    id: "2",
    title: "33. Search in Rotated Sorted Array",
    date: "Novermber 25, 2025",
    difficulty: 3,
    tag: "#HTML, CSS, JS #Webdev #HTML, CSS, JS #Webdev ",
    type: cf,
    link1: cfLink,
    content: [
      `The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.`,
    ],
    code: [
      `#include <iostream>
using namespace std;

int main() {
  cout << "Hello, World!" << endl;
  return 0;
}
  `,
    ],
  },
  {
    id: "3",
    title: "33. Search in Rotated Sorted Array",
    date: "Novermber 25, 2025",
    difficulty: 3,
    tag: "#HTML, CSS, JS #Webdev",
    type: cf,
    link1: cfLink,
    content: [
      `The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.`,
    ],
    code: [
      `#include <iostream>
using namespace std;

int main() {
  cout << "Hello, World!" << endl;
  return 0;
}
  `,
    ],
  },
  {
    id: "4",
    title: "33. Search in Rotated Sorted Array",
    date: "Novermber 25, 2025",
    difficulty: 3,
    tag: "#HTML, CSS, JS #Webdev",
    type: cf,
    link1: cfLink,
    content: [
      `The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.`,
    ],
    code: [
      `#include <iostream>
using namespace std;

int main() {
  cout << "Hello, World!" << endl;
  return 0;
}
  `,
    ],
  },
  {
    id: "5",
    title: "33. Search in Rotated Sorted Array",
    date: "Novermber 25, 2025",
    difficulty: 3,
    tag: "#HTML, CSS, JS #Webdev",
    type: cf,
    link1: cfLink,
    content: [
      `The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.`,
    ],
    code: [
      `#include <iostream>
using namespace std;

int main() {
  cout << "Hello, World!" << endl;
  return 0;
}
  `,
    ],
  },
  {
    id: "6",
    title: "33. Search in Rotated Sorted Array",
    date: "Novermber 25, 2025",
    difficulty: 3,
    tag: "#HTML, CSS, JS #Webdev",
    type: cf,
    link1: cfLink,
    content: [
      `The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.`,
    ],
    code: [
      `#include <iostream>
using namespace std;

int main() {
  cout << "Hello, World!" << endl;
  return 0;
}
  `,
    ],
  },
  {
    id: "7",
    title: "33. Search in Rotated Sorted Array",
    date: "Novermber 25, 2025",
    difficulty: 3,
    tag: "#HTML, CSS, JS #Webdev",
    type: cf,
    link1: cfLink,
    content: [
      `The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.`,
    ],
    code: [
      `#include <iostream>
using namespace std;

int main() {
  cout << "Hello, World!" << endl;
  return 0;
}
  `,
    ],
  },
  {
    id: "8",
    title: "33. Search in Rotated Sorted Array",
    date: "Novermber 25, 2025",
    difficulty: 3,
    tag: "#HTML, CSS, JS #Webdev",
    type: cf,
    link1: cfLink,
    content: [
      `The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.The array is originally sorted but rotated at some pivot index. We need to find the target element using modified binary search.`,
    ],
    code: [
      `#include <iostream>
using namespace std;

int main() {
  cout << "Hello, World!" << endl;
  return 0;
}
  `,
    ],
  },
];
export function findPageAlContent(target) {
  for (let i = 0; i < PageAlContents.length; i++) {
    if (PageAlContents[i].id === target) {
      return PageAlContents[i];
    }
  }
}
